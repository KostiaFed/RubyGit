<!DOCTYPE html>
<html>
  <head>
    <title>Becomer</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <style type="text/css">
    .mme:link, a:active {
    color: #dc3545;
        text-decoration:none
    }
    .mme:visited {
    color: #dc3545;
    font-weight:bold;
        text-decoration:none
    }
    .mme:hover {
    color: #dc3545;
    font-weight:bold;
    text-decoration:none
    }
    .borderio {
        border: 1px solid black;
        padding: 2px
        }
    .marginio {
          margin: 2px
          }
          .topic {
                color: #dc3545
                }
    </style>
  </head>

  <body>
    <header class="p-3 bg-dark text-white">
        <div class="container">
          <div class="d-flex flex-wrap align-items-center justify-content-center justify-content-lg-start">
            <a href="/" class="d-flex align-items-center mb-2 mb-lg-0 text-white text-decoration-none">
<img class="bi me-2" width="40" height="32" src="https://upload.wikimedia.org/wikipedia/commons/f/f1/Ruby_logo.png"/>
            </a>

            <ul class="nav col-12 col-lg-auto me-lg-auto mb-2 justify-content-center mb-md-0">
              <li><span class="px-2 text-white"><%= link_to "Головна", home_path, :class => "mme" %></span></li>
              <li><span class="px-2 text-white"><%= link_to "Створити пост", new_post_path, :class => "mme" %></span></li>
              <li><span class="px-2 text-white"><%= link_to "Створити тему", new_theme_path, :class => "mme" %></span></li>
              <li><span class="px-2 text-white"><%= link_to "Історія", about_path, :class => "mme" %></span></li>

            </ul>

            <form class="col-12 col-lg-auto mb-3 mb-lg-0 me-lg-3">
              <input type="search" class="form-control form-control-dark" placeholder="Пошук..." aria-label="Search">
            </form>

            <div class="text-end">
              <button type="button" class="btn btn-outline-light me-2">Зареєструватись</button>
              <button type="button" class="btn btn-danger">Увійти</button>
            </div>
          </div>
        </div>
      </header>

<div class = "container">
    <%= yield %>
    </div>
  </body>
</html>
